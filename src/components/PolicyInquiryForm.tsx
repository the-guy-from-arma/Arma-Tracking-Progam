"use client";
import Link from "next/link";
import { useState } from "react";
import styles from "./PolicyInquiryForm.module.css";
const categories = [["PRIVACY","Privacy or data request"],["ELECTRONIC_RECORDS","Electronic records or paper copy"],["ACCOUNT_CLOSURE","Account closure"],["BOHEMIA_IP","Bohemia or intellectual property"],["AI_DECISION","Automated academic decision"],["MONETARY_DISCLOSURE","Sponsored-value disclosure"],["ACCESSIBILITY","Accessibility"],["TERMS_DISPUTE","Formal Terms dispute notice"],["OTHER","Other policy question"]];
export function PolicyInquiryForm(){ const [result,setResult]=useState<{trackingNumber:string;accessToken:string|null;statusUrl:string}|null>(null); const [error,setError]=useState(""); const [busy,setBusy]=useState(false); async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/policy-inquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const payload=await response.json();setBusy(false);if(!response.ok)setError(payload.error||"Inquiry could not be created.");else setResult(payload)} if(result)return <main className={styles.complete}><span>POLICY INQUIRY RECORDED</span><h1>{result.trackingNumber}</h1><p>Save this tracking number{result.accessToken ? " and one-time access token" : ""}. No email will be sent.</p>{result.accessToken&&<code>{result.accessToken}</code>}<Link href={result.statusUrl}>Open inquiry status</Link></main>; return <main className={styles.page}><header><Link href="/policies">← POLICY CENTER</Link><span>CONFIDENTIAL INSTITUTIONAL CONTACT</span></header><form onSubmit={submit}><span>POLICY CONTACT SYSTEM</span><h1>Ask, request, or give formal notice.</h1><p>Owner email addresses are not published. This form creates an auditable record and tracking number. A Terms dispute notice begins the 30-day good-faith resolution period.</p><div><label>Your name<input name="name" required minLength={2}/></label><label>Contact email<input name="email" type="email" required/></label></div><label>Category<select name="category">{categories.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label>Subject<input name="subject" required minLength={5}/></label><label>Detailed message<textarea name="message" required minLength={20} rows={8}/></label>{error&&<p className={styles.error}>{error}</p>}<button disabled={busy}>{busy?"CREATING RECORD…":"SUBMIT SECURE INQUIRY"}</button><small>Do not include passwords, API keys, recovery credentials, or unnecessary private information.</small></form></main> }
