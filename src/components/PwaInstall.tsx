"use client";
import { useEffect,useState } from "react";
type Prompt=Event&{prompt:()=>Promise<void>;userChoice:Promise<{outcome:string}>};
export function PwaInstall(){const[prompt,setPrompt]=useState<Prompt|null>(null);const[ios,setIos]=useState(false);const[closed,setClosed]=useState(false);useEffect(()=>{if("serviceWorker"in navigator)navigator.serviceWorker.register("/sw.js").catch(()=>undefined);const standalone=matchMedia("(display-mode: standalone)").matches||Boolean((navigator as Navigator&{standalone?:boolean}).standalone);setIos(/iphone|ipad|ipod/i.test(navigator.userAgent)&&!standalone);const capture=(e:Event)=>{e.preventDefault();setPrompt(e as Prompt)};addEventListener("beforeinstallprompt",capture);return()=>removeEventListener("beforeinstallprompt",capture)},[]);if(closed||(!ios&&!prompt))return null;return <aside className="install"><div><strong>{ios?"ADD FORGEOPS TO IPHONE":"INSTALL FORGEOPS"}</strong><span>{ios?"Safari → Share → Add to Home Screen":"Keep command one tap away."}</span></div>{prompt&&<button onClick={async()=>{await prompt.prompt();await prompt.userChoice;setPrompt(null)}}>INSTALL</button>}<button aria-label="Dismiss" onClick={()=>setClosed(true)}>×</button></aside>}
